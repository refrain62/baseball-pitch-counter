{
  "name": "baseball-pitch-counter",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test": "node --check public/assets/site.js && node --check public/counter/counter.js && node --check public/counter/sw.js",
    "build": "wrangler deploy --dry-run",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.135.0"
  }
}
